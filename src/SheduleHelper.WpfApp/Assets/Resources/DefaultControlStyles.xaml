<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    
    <!-- ================================================ -->
    <!--   DEFAULT (SECONDARY) BUTTON STYLE              -->
    <!--   Material Design Outlined/Text Button          -->
    <!-- ================================================ -->

    <Style x:Key="DefaultButtonStyle" TargetType="{x:Type Button}">
        <!-- Base Properties -->
        <Setter Property="Background" Value="{DynamicResource DefaultControlBackgroundDefaultBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource DefaultControlForegroundDefaultBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DefaultControlBorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="MinWidth" Value="64"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
        
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">

                            <ContentPresenter x:Name="ContentPresenter"
                                            Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            RecognizesAccessKey="True"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>

                        <Border x:Name="FocusBorder"
                                BorderBrush="{DynamicResource AccentPrimaryBrush}"
                                BorderThickness="2"
                                CornerRadius="4"
                                Opacity="0"
                                IsHitTestVisible="False"
                                SnapsToDevicePixels="True"/>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" 
                                    Value="{DynamicResource DefaultControlBackgroundHoverBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" 
                                    Value="{DynamicResource DefaultControlBorderHoverBrush}"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" 
                                    Value="{DynamicResource DefaultControlBackgroundPressedBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" 
                                    Value="{DynamicResource DefaultControlBorderPressedBrush}"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusBorder"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="1"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusBorder"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="0"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MainBorder" Property="Background"
                                    Value="{DynamicResource DefaultControlBackgroundDisabledBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush"
                                    Value="{DynamicResource DefaultControlBorderDisabledBrush}"/>
                            <Setter TargetName="ContentPresenter" Property="Opacity"
                                    Value="{DynamicResource DefaultControlDisabledOpacity}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- ================================================ -->
    <!--   PRIMARY (PROMINENT) BUTTON STYLE              -->
    <!--   Material Design Contained Button              -->
    <!-- ================================================ -->

    <Style x:Key="PrimaryButtonStyle" TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{DynamicResource PrimaryControlBackgroundDefaultBrush}"/>
        <Setter Property="Foreground" Value="{DynamicResource PrimaryControlForegroundDefaultBrush}"/>
        <Setter Property="BorderBrush" Value="{DynamicResource PrimaryControlBorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="16,8"/>
        <Setter Property="MinHeight" Value="36"/>
        <Setter Property="MinWidth" Value="64"/>
        <Setter Property="HorizontalContentAlignment" Value="Center"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="FontWeight" Value="Medium"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <!--<Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    
                </ControlTemplate>
            </Setter.Value>
        </Setter>-->
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Grid>
                        <Border x:Name="MainBorder"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4"
                                SnapsToDevicePixels="True">

                            <ContentPresenter x:Name="ContentPresenter"
                                            Margin="{TemplateBinding Padding}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                            RecognizesAccessKey="True"
                                            SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                        </Border>

                        <Border x:Name="FocusBorder"
                                BorderBrush="{DynamicResource PrimaryControlBackgroundHoverBrush}"
                                BorderThickness="2"
                                CornerRadius="4"
                                Opacity="0"
                                IsHitTestVisible="False"
                                SnapsToDevicePixels="True"/>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" 
                                    Value="{DynamicResource PrimaryControlBackgroundHoverBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" 
                                    Value="{DynamicResource PrimaryControlBorderHoverBrush}"/>
                        </Trigger>

                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="MainBorder" Property="Background" 
                                    Value="{DynamicResource PrimaryControlBackgroundPressedBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush" 
                                    Value="{DynamicResource PrimaryControlBorderPressedBrush}"/>
                        </Trigger>

                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusBorder"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="1"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusBorder"
                                                       Storyboard.TargetProperty="Opacity"
                                                       To="0"
                                                       Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="MainBorder" Property="Background"
                                    Value="{DynamicResource PrimaryControlBackgroundDisabledBrush}"/>
                            <Setter TargetName="MainBorder" Property="BorderBrush"
                                    Value="{DynamicResource PrimaryControlBorderDisabledBrush}"/>
                            <Setter TargetName="ContentPresenter" Property="Opacity"
                                    Value="{DynamicResource PrimaryControlDisabledOpacity}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}"/>

    <!-- ================================================ -->
    <!--   DEFAULT CHECKBOX STYLE                        -->
    <!--   Material Design Checkbox                       -->
    <!-- ================================================ -->

    <Style x:Key="DefaultCheckBoxStyle" TargetType="{x:Type CheckBox}">
        <!-- Base Properties -->
        <Setter Property="Foreground" Value="{DynamicResource DefaultControlForegroundDefaultBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DefaultControlBorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="8,0,0,0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type CheckBox}">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- CheckBox Visual -->
                        <Grid Width="18" Height="18" VerticalAlignment="Center">
                            <!-- Unchecked Border -->
                            <Border x:Name="CheckBoxBorder"
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="3"
                                    SnapsToDevicePixels="True"/>

                            <!-- Checked Background -->
                            <Border x:Name="CheckedBackground"
                                    Background="{DynamicResource AccentPrimaryBrush}"
                                    BorderBrush="{DynamicResource AccentPrimaryBrush}"
                                    BorderThickness="2"
                                    CornerRadius="3"
                                    Opacity="0"
                                    SnapsToDevicePixels="True"/>

                            <!-- Check Mark (✓) -->
                            <Path x:Name="CheckMark"
                                  Data="M 0,4 L 3,7 L 8,0"
                                  Stroke="White"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  StrokeLineJoin="Round"
                                  Stretch="Uniform"
                                  Margin="3"
                                  Opacity="0"
                                  SnapsToDevicePixels="True"/>

                            <!-- Indeterminate Mark (—) -->
                            <Path x:Name="IndeterminateMark"
                                  Data="M 0,0 L 8,0"
                                  Stroke="White"
                                  StrokeThickness="2"
                                  StrokeStartLineCap="Round"
                                  StrokeEndLineCap="Round"
                                  Stretch="Uniform"
                                  Margin="3"
                                  VerticalAlignment="Center"
                                  Opacity="0"
                                  SnapsToDevicePixels="True"/>

                            <!-- Focus Border -->
                            <Border x:Name="FocusBorder"
                                    BorderBrush="{DynamicResource AccentPrimaryBrush}"
                                    BorderThickness="2"
                                    CornerRadius="3"
                                    Opacity="0"
                                    IsHitTestVisible="False"
                                    SnapsToDevicePixels="True"/>
                        </Grid>

                        <!-- Content -->
                        <ContentPresenter x:Name="ContentPresenter"
                                          Grid.Column="1"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <!-- Hover State -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush"
                                    Value="{DynamicResource DefaultControlBorderHoverBrush}"/>
                        </Trigger>

                        <!-- Pressed State -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush"
                                    Value="{DynamicResource DefaultControlBorderPressedBrush}"/>
                        </Trigger>

                        <!-- Checked State -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckedBackground" Property="Opacity" Value="1"/>
                            <Setter TargetName="CheckMark" Property="Opacity" Value="1"/>
                            <Setter TargetName="CheckBoxBorder" Property="Opacity" Value="0"/>
                        </Trigger>

                        <!-- Checked + Hover -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedBackground" Property="Background"
                                    Value="{DynamicResource AccentPrimaryHoverBrush}"/>
                            <Setter TargetName="CheckedBackground" Property="BorderBrush"
                                    Value="{DynamicResource AccentPrimaryHoverBrush}"/>
                        </MultiTrigger>

                        <!-- Checked + Pressed -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedBackground" Property="Background"
                                    Value="{DynamicResource AccentPrimaryPressedBrush}"/>
                            <Setter TargetName="CheckedBackground" Property="BorderBrush"
                                    Value="{DynamicResource AccentPrimaryPressedBrush}"/>
                        </MultiTrigger>

                        <!-- Indeterminate State -->
                        <Trigger Property="IsChecked" Value="{x:Null}">
                            <Setter TargetName="CheckedBackground" Property="Opacity" Value="1"/>
                            <Setter TargetName="IndeterminateMark" Property="Opacity" Value="1"/>
                            <Setter TargetName="CheckBoxBorder" Property="Opacity" Value="0"/>
                        </Trigger>

                        <!-- Indeterminate + Hover -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="{x:Null}"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedBackground" Property="Background"
                                    Value="{DynamicResource AccentPrimaryHoverBrush}"/>
                            <Setter TargetName="CheckedBackground" Property="BorderBrush"
                                    Value="{DynamicResource AccentPrimaryHoverBrush}"/>
                        </MultiTrigger>

                        <!-- Indeterminate + Pressed -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="{x:Null}"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedBackground" Property="Background"
                                    Value="{DynamicResource AccentPrimaryPressedBrush}"/>
                            <Setter TargetName="CheckedBackground" Property="BorderBrush"
                                    Value="{DynamicResource AccentPrimaryPressedBrush}"/>
                        </MultiTrigger>

                        <!-- Focus State -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusBorder"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="CheckBoxBorder" Property="Background"
                                    Value="{DynamicResource DefaultControlBackgroundDisabledBrush}"/>
                            <Setter TargetName="CheckBoxBorder" Property="BorderBrush"
                                    Value="{DynamicResource DefaultControlBorderDisabledBrush}"/>
                            <Setter TargetName="ContentPresenter" Property="Opacity"
                                    Value="{DynamicResource DefaultControlDisabledOpacity}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>

                        <!-- Disabled + Checked -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedBackground" Property="Background"
                                    Value="{DynamicResource AccentPrimaryDisabledBrush}"/>
                            <Setter TargetName="CheckedBackground" Property="BorderBrush"
                                    Value="{DynamicResource AccentPrimaryDisabledBrush}"/>
                            <Setter TargetName="CheckMark" Property="Opacity" Value="0.6"/>
                        </MultiTrigger>

                        <!-- Disabled + Indeterminate -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsChecked" Value="{x:Null}"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedBackground" Property="Background"
                                    Value="{DynamicResource AccentPrimaryDisabledBrush}"/>
                            <Setter TargetName="CheckedBackground" Property="BorderBrush"
                                    Value="{DynamicResource AccentPrimaryDisabledBrush}"/>
                            <Setter TargetName="IndeterminateMark" Property="Opacity" Value="0.6"/>
                        </MultiTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="CheckBox" BasedOn="{StaticResource DefaultCheckBoxStyle}"/>

    <!-- ================================================ -->
    <!--   DEFAULT RADIOBUTTON STYLE                     -->
    <!--   Material Design RadioButton                    -->
    <!-- ================================================ -->

    <Style x:Key="DefaultRadioButtonStyle" TargetType="{x:Type RadioButton}">
        <!-- Base Properties -->
        <Setter Property="Foreground" Value="{DynamicResource DefaultControlForegroundDefaultBrush}"/>
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="BorderBrush" Value="{DynamicResource DefaultControlBorderDefaultBrush}"/>
        <Setter Property="BorderThickness" Value="2"/>
        <Setter Property="Padding" Value="8,0,0,0"/>
        <Setter Property="FontSize" Value="14"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="HorizontalContentAlignment" Value="Left"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="FocusVisualStyle" Value="{x:Null}"/>

        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type RadioButton}">
                    <Grid Background="Transparent">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- RadioButton Visual -->
                        <Grid Width="18" Height="18" VerticalAlignment="Center">
                            <!-- Outer Circle (Unchecked Border) -->
                            <Ellipse x:Name="OuterCircle"
                                     Stroke="{TemplateBinding BorderBrush}"
                                     StrokeThickness="{TemplateBinding BorderThickness}"
                                     Fill="{TemplateBinding Background}"
                                     SnapsToDevicePixels="True"/>

                            <!-- Checked Outer Circle -->
                            <Ellipse x:Name="CheckedOuterCircle"
                                     Stroke="{DynamicResource AccentPrimaryBrush}"
                                     StrokeThickness="2"
                                     Fill="Transparent"
                                     Opacity="0"
                                     SnapsToDevicePixels="True"/>

                            <!-- Inner Circle (Checked Indicator) -->
                            <Ellipse x:Name="InnerCircle"
                                     Fill="{DynamicResource AccentPrimaryBrush}"
                                     Width="8"
                                     Height="8"
                                     HorizontalAlignment="Center"
                                     VerticalAlignment="Center"
                                     Opacity="0"
                                     SnapsToDevicePixels="True"/>

                            <!-- Focus Circle -->
                            <Ellipse x:Name="FocusCircle"
                                     Stroke="{DynamicResource AccentPrimaryBrush}"
                                     StrokeThickness="2"
                                     Fill="Transparent"
                                     Opacity="0"
                                     IsHitTestVisible="False"
                                     SnapsToDevicePixels="True"/>
                        </Grid>

                        <!-- Content -->
                        <ContentPresenter x:Name="ContentPresenter"
                                          Grid.Column="1"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                          VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                          RecognizesAccessKey="True"
                                          SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                    </Grid>

                    <ControlTemplate.Triggers>

                        <!-- Hover State -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="OuterCircle" Property="Stroke"
                                    Value="{DynamicResource DefaultControlBorderHoverBrush}"/>
                        </Trigger>

                        <!-- Pressed State -->
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="OuterCircle" Property="Stroke"
                                    Value="{DynamicResource DefaultControlBorderPressedBrush}"/>
                        </Trigger>

                        <!-- Checked State -->
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="CheckedOuterCircle" Property="Opacity" Value="1"/>
                            <Setter TargetName="InnerCircle" Property="Opacity" Value="1"/>
                            <Setter TargetName="OuterCircle" Property="Opacity" Value="0"/>
                        </Trigger>

                        <!-- Checked + Hover -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsMouseOver" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedOuterCircle" Property="Stroke"
                                    Value="{DynamicResource AccentPrimaryHoverBrush}"/>
                            <Setter TargetName="InnerCircle" Property="Fill"
                                    Value="{DynamicResource AccentPrimaryHoverBrush}"/>
                        </MultiTrigger>

                        <!-- Checked + Pressed -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsChecked" Value="True"/>
                                <Condition Property="IsPressed" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedOuterCircle" Property="Stroke"
                                    Value="{DynamicResource AccentPrimaryPressedBrush}"/>
                            <Setter TargetName="InnerCircle" Property="Fill"
                                    Value="{DynamicResource AccentPrimaryPressedBrush}"/>
                        </MultiTrigger>

                        <!-- Focus State -->
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Trigger.EnterActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusCircle"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="1"
                                                         Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.EnterActions>
                            <Trigger.ExitActions>
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="FocusCircle"
                                                         Storyboard.TargetProperty="Opacity"
                                                         To="0"
                                                         Duration="0:0:0.2">
                                            <DoubleAnimation.EasingFunction>
                                                <CubicEase EasingMode="EaseOut"/>
                                            </DoubleAnimation.EasingFunction>
                                        </DoubleAnimation>
                                    </Storyboard>
                                </BeginStoryboard>
                            </Trigger.ExitActions>
                        </Trigger>

                        <!-- Disabled State -->
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter TargetName="OuterCircle" Property="Fill"
                                    Value="{DynamicResource DefaultControlBackgroundDisabledBrush}"/>
                            <Setter TargetName="OuterCircle" Property="Stroke"
                                    Value="{DynamicResource DefaultControlBorderDisabledBrush}"/>
                            <Setter TargetName="ContentPresenter" Property="Opacity"
                                    Value="{DynamicResource DefaultControlDisabledOpacity}"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>

                        <!-- Disabled + Checked -->
                        <MultiTrigger>
                            <MultiTrigger.Conditions>
                                <Condition Property="IsEnabled" Value="False"/>
                                <Condition Property="IsChecked" Value="True"/>
                            </MultiTrigger.Conditions>
                            <Setter TargetName="CheckedOuterCircle" Property="Stroke"
                                    Value="{DynamicResource AccentPrimaryDisabledBrush}"/>
                            <Setter TargetName="InnerCircle" Property="Fill"
                                    Value="{DynamicResource AccentPrimaryDisabledBrush}"/>
                            <Setter TargetName="InnerCircle" Property="Opacity" Value="0.6"/>
                        </MultiTrigger>

                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="RadioButton" BasedOn="{StaticResource DefaultRadioButtonStyle}"/>
</ResourceDictionary>